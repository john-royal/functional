{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["VITE_API_URL"],
      "inputs": ["app.config.ts", "package.json", "public/**", "src/**"],
      "outputs": [".output/**", ".vinxi/**"]
    }
  }
}
